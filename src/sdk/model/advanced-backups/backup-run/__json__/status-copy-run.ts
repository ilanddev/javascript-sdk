/**
 * Status Copy Run Enum.
 */
export enum StatusCopyRun {
  ACCEPTED = 'ACCEPTED',
  RUNNING = 'RUNNING',
  CANCELING = 'CANCELING',
  CANCELED = 'CANCELED',
  SUCCESS = 'SUCCESS',
  FAILURE = 'FAILURE'
}
